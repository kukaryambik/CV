name: MD to PDF

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
    # paths:
    #   - '**.md'

jobs:
  converttopdf:
    name: Build PDF
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: apt-get update && apt-get install -y pandoc
      - run: pandoc README.md -o cv.pdf
      - uses: actions/upload-artifact@v4
        with:
          name: CV
          path: cv.pdf
